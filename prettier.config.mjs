import sharedConfig from './prettier.shared.mjs';

export default {
  ...sharedConfig,
  // Add consumer-specific Prettier overrides here. This file is copied only once so local edits are preserved.
};
