import sharedConfig from './eslint.shared.mjs';

export default [
  ...sharedConfig,
  {
    // Add consumer-specific ESLint rules here. This file is copied only once so local edits are preserved.
    rules: {},
  },
];
